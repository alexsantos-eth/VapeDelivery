import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  image: {
    width: '100%',
    height: '100%',
  },

  imageContainer: {
    height: 20,
    width: 20,
  },
});

export default style;
