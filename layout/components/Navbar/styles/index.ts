import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    position: 'relative',
    zIndex: 100,
  },
  image: {
    width: '100%',
    height: '100%',
  },

  imageContainer: {
    height: 20,
    width: 20,
  },
});

export default styles;
