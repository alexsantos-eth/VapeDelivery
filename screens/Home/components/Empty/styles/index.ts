import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
  },

  imageContainer: {
    width: 200,
    height: 200,
  },

  image: {
    width: '100%',
    height: '100%',
  },
});

export default styles;
